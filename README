#set -x

# This script was created to automate terminal commands for implementation of settings as outlined in:
#
#       CIS Apple OSX 10.x Benchmark
#

# Open Terminal and navigate to the directory in which it resides then run the following command:
 chmod 755 *.sh
 To check the compliant current state 
 ./audit.sh > current.html


# To run the remediation 
 ./system_setup.sh

# There are few exception on this CIS 
2.6.1 Enable FileVault - manual configuration
5.3 Reduce the sudo timeout period - from 10.11 to 10.14 We cannot edit directly into Sudoers.
